import CustomButton from "../../components/CustomButton";

const Dashboard = () => {
  return (
    <div>
      Dashboard
      <CustomButton variant="custom" />
    </div>
  );
};

export default Dashboard;